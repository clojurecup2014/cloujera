define(["backbone","underscore","bundles/assess/framework/QuestionModel"],function(Backbone,_,QuestionModel){var QuestionCollection=Backbone.Collection.extend({model:QuestionModel,addMerge:function(data){var self=this;_(data).each(function(datum){var model=self.get(datum.id);if(model)model.set(datum);else self.add(new self.model(datum))})},toResponseJson:function(){return this.invoke("toResponseJson")}});return QuestionCollection});